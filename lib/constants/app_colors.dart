import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const background = Color(0xFF0D1117);
  static const surface = Color(0xFF161B22);
  static const divider = Color(0xFF30363D);
  static const buttonBg = Color(0xFF21262D);

  // Text colors
  static const textPrimary = Colors.white;
  static const textSecondary = Color(0xFFC9D1D9);
  static const textMuted = Color(0xFF8B949E);

  // Action colors
  static const primary = Color(0xFF58A6FF);
  static const tabIndicator = Color(0xFFFA7A18);
}